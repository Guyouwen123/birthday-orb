# 生日祝福星球

一个可互动的生日祝福网页：中心是滚动祝福球，每个五边形/六边形碎片都带一个祝福字，选中后会放大并展开完整祝福语。

## 启动

```powershell
D:\Node\node.exe server.js
```

然后打开：

```text
http://127.0.0.1:5173/
```

## 交互

- 拖拽球体可以旋转。
- 点击或悬停任意祝福片会放大并展开祝福。
- 点击相机按钮后，浏览器会请求摄像头权限；允许后可以用食指指向祝福片进行选择。
- 如果手势模型无法加载，鼠标和触控交互仍然可用。
